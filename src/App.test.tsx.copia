import * as React from 'react';
import * as ReactDOM from 'react-dom';
import Vrview from './Vrview';

it('renders without crashing', () => {
  const div = document.createElement('div');
  ReactDOM.render(<Vrview />, div);
});
